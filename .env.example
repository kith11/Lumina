# Layerbase provides a pooled PostgreSQL-compatible URL for its hosted SQLite database.
# Keep the real value in .env.local, .env, or lumina-credentials.env; never commit it.
DATABASE_URL=postgresql://user:password@your-host.cloud.layerbase.dev:5432/sqlite1?sslmode=require
